using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Moving {

    //public void Action() {

    //    if(CheckOpportunity() ) {
    //        Logic();
    //        Vizual();
    //    }
       
    //}

    private bool CheckOpportunity( ) {

        //move direction; // 1- можно переместить 2- нельзя

        //if (NewElement == null)
        //    return false;

        //if (direction == move.x) {
        //    foreach (var item in NewElement.MyBlocks) {
        //        if (item.x == Mathf.Abs(MinCoordinat))
        //            return false;

        //        if (_matrix._matrix[item.x + 1 + 1, item.y, item.z + 1] != null)
        //            return false;
        //    }

        //    return true;
        //}
        //else if (direction == move._x) {
        //    foreach (var item in NewElement.MyBlocks) {
        //        if (item.x == MinCoordinat)
        //            return false;

        //        if (_matrix._matrix[item.x + 1 - 1, item.y, item.z + 1] != null)
        //            return false;
        //    }

        //    return true;
        //}
        //else if (direction == move.z) {
        //    foreach (var item in NewElement.MyBlocks) {
        //        if (item.z == Mathf.Abs(MinCoordinat))
        //            return false;

        //        if (_matrix._matrix[item.x + 1, item.y, item.z + 1 + 1] != null)
        //            return false;
        //    }

        //    return true;
        //}
        //else if (direction == move._z) {
        //    foreach (var item in NewElement.MyBlocks) {
        //        if (item.z == MinCoordinat)
        //            return false;

        //        if (_matrix._matrix[item.x + 1, item.y, item.z + 1 - 1] != null)
        //            return false;
        //    }

        //    return true;
        //}

        return true;
    
}


private void Logic() {
}

private void Vizual() {
}
    
}
